<str>{

\"          {
                if (!d_block)
                    return end(Parser::STRING);
                d_block += matched();
                end();
            }
            
\\.         |
.           more(); 
            
\n          return end(-1);

}

