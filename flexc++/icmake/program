
void program(string strip, int program_only)
{
    string lopt;

    library(0);

    lopt = strip + setOpt(LDFLAGS, "LDFLAGS");

    exec(COMPILER, "-o", g_binary, "-lflexc++", libs, "-L.", libpath , lopt);

    if (program_only)
        exit(0);
}
