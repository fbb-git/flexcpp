<patterntxt>{
    [[:blank:]]+        return Parser::WS;

    \n                  return '\n';

    "\""                {
                            more();
                            begin(StartCondition__::str);
                        }

    [^[:blank:]\n]+     return Parser::STRING;
}
