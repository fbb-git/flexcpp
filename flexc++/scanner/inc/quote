<quote>{
    '                       {
                                if (d_block)
                                    d_block += d_match;
                                end();
                            }

    \\.                     |
    .                       more(); 

    \n                      return end(-1);     // error-generating token
}

