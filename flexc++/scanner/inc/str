<str>{

\"          {
                if (!d_block)
                    return end(Parser::STRING);
                d_block += d_match;
                end();
            }
            
\\.         |
.           more(); 
            
\n          return end(-1);

}

